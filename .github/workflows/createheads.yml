name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: |
          ls
          cd heads
          echo "bee4bee is very evil and malicious </3" > bee4bee
          git commit -am "bee4bee hacked gameherobrines acc"
          git push
